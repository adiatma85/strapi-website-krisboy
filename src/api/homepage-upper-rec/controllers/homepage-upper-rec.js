'use strict';

/**
 *  homepage-upper-rec controller
 */

const { createCoreController } = require('@strapi/strapi').factories;

module.exports = createCoreController('api::homepage-upper-rec.homepage-upper-rec');
