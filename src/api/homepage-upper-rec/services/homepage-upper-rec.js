'use strict';

/**
 * homepage-upper-rec service.
 */

const { createCoreService } = require('@strapi/strapi').factories;

module.exports = createCoreService('api::homepage-upper-rec.homepage-upper-rec');
